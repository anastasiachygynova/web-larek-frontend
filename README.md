# Проектная работа "Веб-ларёк" 🛒

## 📝 Описание

Интернет-магазин с товарами для веб-разработчиков — Web-ларёк. В нём можно посмотреть каталог товаров, добавить товары в корзину и сделать заказ. В проекте используется классовая структура и архитектура MVP.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
# или
yarn
```

### Запуск в режиме разработки

```bash
npm run start
# или
yarn start
```

## 🗂️ Ключевые файлы

- `src/pages/index.html` — HTML-файл главной страницы
- `src/types/index.ts` — файл с типами
- `src/index.ts` — точка входа приложения
- `src/scss/styles.scss` — корневой файл стилей
- `src/utils/constants.ts` — файл с константами
- `src/utils/utils.ts` — файл с утилитами

---

## 🛠️ Технологический стек

- **Frontend:** HTML, SCSS, TypeScript
- **Сборка:** Webpack, PostCSS, Babel
- **Пакетный менеджер:** npm/yarn
- **Форматирование:** Prettier
- **Среда выполнения:** Node.js

---

## 🏗️ Архитектура

В проекте используется классовая структура — сущности реализованы как классы, которые инкапсулируют данные и методы. Архитектура построена по принципу **MVP**:

- **Model** — отвечает за данные
- **View (Компоненты)** — отвечает за отображение
- **Presenter** — посредник между Model и View, управляет логикой взаимодействия, обрабатывает события, обновляет View

### Классы Model

#### ApiModel

**Назначение** — наследуется от класса `Api`, обеспечивает взаимодействие с сервером для получения и отправки данных.
**Поля класса:**
- cdn — адрес CDN-сервера
- items — массив товаров (IProduct[])
**Конструктор:**
- constructor(cdn: string, baseUrl: string, options?: RequestInit) — принимает строку с адресом CDN, строку с базовым адресом API и объект с дополнительными настройками для HTTP-запросов
**Методы:**
- getProductList — получает массив карточек товаров с сервера
- placeOrder — отправляет заказ на сервер и получает результат обработки

#### BasketModel

**Назначение** — управляет данными корзины покупок, хранит и обрабатывает информацию о выбранных товарах.
**Поля класса:**
- _items — массив товаров в корзине (IProduct[])
**Конструктор:**
- инициализирует пустой массив items (типа IProduct[]), который будет хранить товары в корзине
**Методы:**
- getCount — возвращает количество валидных товаров в корзине
- getTotal — возвращает общую стоимость всех валидных товаров в корзине
- addProduct — добавляет товар в корзину, если его там ещё нет и у него есть id
- removeProduct — удаляет товар из корзины по id
- clear — полностью очищает корзину
- cleanInvalidItems — удаляет из корзины все невалидные товары

#### DataCatalog
**Назначение** — хранит и управляет данными о товарах, полученными с сервера.
**Поля класса:**
- _products — массив всех продуктов каталога (IProduct[])
- selectedProduct — выбранный продукт для предпросмотра (IProduct | null)
**Конструктор:**
- constructor(protected events: IEvents) — принимает объект events типа IEvents, который используется для генерации событий внутри класса
**Методы:**
- set products — устанавливает массив товаров и эмиттит событие 'products:receive'
- get products — возвращает текущий массив товаров
- setPreview — устанавливает выбранный товар и эмиттит событие 'modalProduct:open' с этим товаром

#### OrderForm
**Назначение** — управляет данными форм заказа, включая валидацию пользовательского ввода.
**Поля класса:**
- payment — способ оплаты (string)
- email — email пользователя (string)
- phone — телефон пользователя (string)
- address — адрес доставки (string)
- total — итоговая сумма заказа (number)
- items — список идентификаторов товаров в заказе (string[])
- formErrors — объект с ошибками валидации формы (FormErrors)
- events — объект для работы с событиями (IEvents, protected)
**Конструктор:**
- constructor(protected events: IEvents) — принимает объект events, который используется для оповещения других частей приложения о результатах валидации
**Методы:**
- setAddress — устанавливает адрес и запускает валидацию адреса и оплаты
- setPayment — устанавливает способ оплаты и запускает валидацию адреса и оплаты
- setEmail — устанавливает email и запускает валидацию контактов
- setPhone — устанавливает телефон и запускает валидацию контактов
- setItems — устанавливает список товаров
- setTotal — устанавливает итоговую сумму заказа
- clear — очищает все поля формы
- validateAddressAndPayment — проверяет корректность адреса и способа оплаты

#### ProductCardsModel
**Назначение** — отвечает за хранение и управление списком карточек товаров
**Поля класса:**
- productCards — массив объектов товаров, которые хранятся в модели (private)
- events — объект для работы с событиями (protected)
**Конструктор:**
- constructor(protected events: IEvents) — принимает объект events и сохраняет его в поле класса
**Методы:**
- get productCards — возвращает текущий массив карточек товаров
- setProductCards — устанавливает новый массив карточек товаров и вызывает событие 'productCards:receive' через events

### Классы View
#### AppView
**Назначение** — основной класс представления (View) для управления пользовательским интерфейсом приложения интернет-магазина.
**Поля класса:**
- products — массив товаров, полученных с сервера
- basketView — объект для отображения и управления корзиной
- eventEmitter — объект для работы с событиями
- apiModel — модель для работы с API (получение товаров, оформление заказа)
- basketModel — модель корзины (добавление/удаление товаров, подсчет суммы)
- dataCatalog — каталог товаров (хранит список товаров)
- modal — объект для управления модальными окнами
- orderForm — объект для хранения и валидации данных заказа
- currentPaymentModalView — текущий модал оплаты (для управления состоянием и валидацией)
- currentContactsModalView — текущий модал контактов (для управления состоянием и валидацией)
**Конструктор:**
- constructor(eventEmitter: EventEmitter, apiModel: ApiModel, basketModel: BasketModel, dataModel: DataCatalog) — инициализация всех полей через параметры конструктора и создание новых экземпляров зависимых классов
**Методы:**
- init — асинхронно загружает список товаров, фильтрует их и сохраняет в каталог, настраивает кнопку корзины
- setupEventListeners — подписывает обработчики на события (добавление/удаление товаров, открытие модалок, валидация и т.д.)
- setupBasketButton — настраивает обработчик клика по кнопке корзины
- updateBasketCounter — обновляет счетчик товаров в корзине в шапке
- openProductModal — открывает модальное окно с подробной информацией о товаре
- openBasketModal — открывает модальное окно с содержимым корзины
- openOrderModal — открывает модальное окно для ввода адреса и выбора способа оплаты
- openContactsModal — открывает модальное окно для ввода контактных данных
- openSuccessModal — открывает модальное окно с сообщением об успешном оформлении заказа
- fillOrderForm — заполняет форму заказа актуальными данными из корзины
- submitOrder — отправляет заказ на сервер, обрабатывает ошибки
- showFormErrors — отображает ошибки валидации форм
- getTemplate — возвращает HTML-шаблон по селектору
- handleFormErrors — управляет состоянием кнопки отправки формы и отображением ошибок

#### BasketView

**Назначение** — управляет отображением содержимого в корзине.
**Поля класса:**
- element — корневой элемент корзины (DOM-элемент, который содержит всю корзину)
- titleElement — элемент заголовка модального окна корзины
- listElement — элемент, в который добавляются товары корзины
- orderButton — кнопка оформления заказа
- priceElement — элемент, в котором отображается итоговая сумма заказа
- headerButton — кнопка корзины в шапке сайта
- headerCounter — счетчик количества товаров в корзине в шапке сайта
**Конструктор:**
- конструктор клонирует шаблон корзины, находит и сохраняет нужные элементы, навешивает обработчики событий на кнопки и инициализирует корзину пустым массивом товаров
**Методы:**
- set items — обновляет содержимое корзины. Если массив не пустой — отображает товары и активирует кнопку заказа, если пустой — показывает сообщение "Корзина пуста" и блокирует кнопку заказа
- updateHeaderCounter — обновляет счетчик товаров в шапке
- updateTotalPrice — обновляет итоговую сумму заказа
- render — возвращает DOM-элемент корзины и устанавливает заголовок "Корзина"

#### MainPageView
**Назначение** — отвечает за отображение главной страницы с каталогом товаров и взаимодействие с корзиной в приложении. Реализует визуализацию списка товаров, обработку кликов по карточкам товаров и кнопке корзины.
**Поля класса:**
- products — массив товаров, которые будут отображаться на главной странице
- eventEmitter — экземпляр класса для работы с событиями
**Конструктор:**
- constructor(eventEmitter: EventEmitter) — принимает объект eventEmitter и сохраняет его в соответствующее поле. Вызывает setupBasketButton(), который навешивает обработчик на кнопку корзины в шапке страницы
**Методы:**
- setProducts — принимает массив товаров, фильтрует их, сохраняет и вызывает рендеринг товаров
- renderProducts — отвечает за отрисовку карточек товаров в контейнере .gallery
- setupBasketButton — находит кнопку корзины и навешивает на нее обработчик клика
- updateBasketCounter — обновляет счетчик товаров в корзине в DOM (элемент с классом .header__basket-counter)

#### ModalView

**Назначение** — управляет отображением модального окна на веб-странице. Позволяет открывать и закрывать окно, а также автоматически добавляет обработчик для кнопки закрытия.
**Поля класса:**
- modalContainer — DOM-элемент модального окна
- closeButton — кнопка закрытия модального окна
- _content — контейнер для динамического контента модального окна
- _pageWrapper — основной контейнер страницы, который блокируется при открытии модалки
- events — объект для работы с событиями (emit)
**Конструктор:**
- constructor(modalContainer: HTMLElement, protected events: IEvents) — сохраняет ссылки на основные DOM-элементы модального окна. Назначает обработчики событий для закрытия модалки. Сохраняет events для дальнейшего использования
**Методы:**
- open — открывает модальное окно
- close — закрывает модальное окно

#### OrderModalView
**Назначение** — класс для модального окна оформления заказа, который переключает этапы между оплатой и вводом контактов.
**Поля класса:**
- currentStep — текущий этап модального окна
- paymentView — экземпляр компонента для этапа оплаты
- contactsView — экземпляр компонента для этапа контактов
**Конструктор:**
- constructor(paymentTemplate: HTMLTemplateElement, contactsTemplate: HTMLTemplateElement, protected events: IEvents) — создаёт и инициализирует подмодальные окна для этапов оплаты и контактов
**Методы:**
- setStep — переключает текущий этап (payment или contacts)
- render — возвращает DOM-элемент для текущего этапа (paymentView или contactsView)

#### OrderSuccessModal

**Назначение** — отображает модальное окно успешного заказа.
**Поля класса:**
- success — DOM-элемент модального окна успеха (клонируется из шаблона)
- description — DOM-элемент, в который будет подставляться текст с итоговой суммой
- button — кнопка закрытия модального окна
**Конструктор:**
- constructor(template: HTMLTemplateElement, protected events: IEvents) — клонирует шаблон успешного заказа, находит DOM-элементы, устанавливает обработчик события на кнопку
**Методы:**
- render — устанавливает текст с указанием суммы (Списано ${total} синапсов) и возвращает готовый DOM-элемент (this.success) для вставки на страницу

#### ProductView
**Назначение** — отвечает за отображение карточки продукта на странице.
**Поля класса:**
- _cardElement — главный элемент карточки, клонируется из шаблона
- _cardCategory — элемент, отображающий категорию продукта
- _cardTitle — элемент, отображающий название продукта
- _cardImage — элемент изображения продукта
- _cardPrice — элемент, отображающий цену продукта
- _colors — цветной элемент на карточке
**Методы:**
- setTextContent — устанавливает текстовое содержимое для переданного элемента
- set cardCategory — обновляет текст и CSS-класс категории в зависимости от значения
- formatPrice — форматирует цену: если null — возвращает "Бесценно", иначе — "{число} синапсов"
- render — метод для отображения карточки

#### ProductPreview
**Назначение** — класс для отображения подробной информации о товаре и управления его добавлением/удалением из корзины. Наследуется от ProductView.
**Поля класса:**
- text — элемент для отображения описания товара
- button — кнопка для добавления/удаления товара из корзины
- currentProduct — текущий отображаемый товар
**Конструктор:**
- constructor(template: HTMLTemplateElement, protected events: IEvents, actions?: IActions) — вызывает конструктор родителя, инициализирует поля text и button. Для button добавляется обработчик клика
**Методы:**
- render — отображает товар, обновляет содержимое карточки, настраивает кнопку

## Базовые классы:

### 📦 Класс Api
**Назначение:** — инкапсулирует работу с серверным API. Предоставляет методы для выполнения HTTP-запросов и обработки ответов.
**Поля класса:**
- baseUrl — базовый URL для всех запросов
- options — объект с настройками для fetch-запросов (`RequestInit`)
**Конструктор:**
- constructor(baseUrl: string, options: RequestInit = {})
**Методы:**
- handleResponse — обработка ответа от сервера
- get — выполняет GET-запрос по указанному URI
- post — выполняет POST/PUT/DELETE-запрос с передачей данных

### 🔔 Класс EventEmitter

**Назначение:** — обеспечивает работу событий. Позволяет объектам обмениваться событиями без прямой зависимости друг от друга.
**Поля класса:**
- _events — приватное поле, представляющее собой объект типа Map<EventName, Set<Subscriber>>
**Конструктор:**
- constructor() — инициализирует _events как new Map<EventName, Set<Subscriber>>()
**Методы:**
- on — подписывает обработчик на событие
- off — удаляет обработчик события
- emit — вызывает все обработчики, подписанные на событие
- onAll — подписывает обработчик на все события
- offAll — удаляет все обработчики всех событий
- trigger — возвращает функцию, которая при вызове инициирует событие с аргументами

---

