# Проектная работа "Веб-ларёк" 🛒

---

## 📝 Описание

Интернет-магазин с товарами для веб-разработчиков — Web-ларёк. В нём можно посмотреть каталог товаров, добавить товары в корзину и сделать заказ.
---

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
# или
yarn
```

### Запуск в режиме разработки

```bash
npm run start
# или
yarn start
```

---

## 🗂️ Ключевые файлы

- `src/pages/index.html` — HTML-файл главной страницы
- `src/types/index.ts` — файл с типами
- `src/index.ts` — точка входа приложения
- `src/scss/styles.scss` — корневой файл стилей
- `src/utils/constants.ts` — файл с константами
- `src/utils/utils.ts` — файл с утилитами

---

## 🛠️ Технологический стек

- **Frontend:** HTML, SCSS, TypeScript
- **Сборка:** Webpack, PostCSS, Babel
- **Пакетный менеджер:** npm/yarn
- **Форматирование:** Prettier
- **Среда выполнения:** Node.js

---

## 🏗️ Архитектура

В проекте используется классовая структура — сущности реализованы как классы, которые инкапсулируют данные и методы. Архитектура построена по принципу **MVP**:

- **Model** — отвечает за данные
- **View (Компоненты)** — отвечает за отображение
- **Presenter** — посредник между Model и View, управляет логикой взаимодействия, обрабатывает события, обновляет View.

### Классы Model

#### ApiModel

**Назначение:**

Класс `ApiModel` отвечает за взаимодействие с серверным API для получения списка товаров и оформления заказа. Он расширяет базовый класс `Api`, добавляя работу с CDN для изображений товаров и реализует методы для получения данных и отправки заказов.

**Поля класса:**
- `cdn: string` — адрес CDN-сервера, который используется для формирования абсолютных ссылок на изображения товаров.
- `items: IProduct[]` — массив товаров, полученных с сервера. По умолчанию — пустой массив.

**Конструктор:**
```ts
constructor(cdn: string, baseUrl: string, options?: RequestInit)
```
- `cdn` — строка с адресом CDN-сервера.
- `baseUrl` — базовый URL для API-запросов.
- `options` — дополнительные настройки для HTTP-запросов.

> Конструктор вызывает конструктор родительского класса `Api` с базовым URL и опциями, а также сохраняет адрес CDN в поле `cdn`.

**Методы:**
- `getProductList(): Promise<IProduct[]>` — получает список товаров с сервера. Возвращает промис с массивом товаров.
- `placeOrder(order: IOrderRequest): Promise<IOrderResult>` — отправляет заказ на сервер. Принимает объект заказа. Возвращает промис с результатом оформления заказа (`IOrderResult`).



---

### Класс: `BasketModel`

Модель корзины интернет-магазина. Отвечает за хранение, управление и обработку товаров, добавленных пользователем в корзину. Использует событийную модель для оповещения других компонентов о любых изменениях в корзине.

#### Поля

- **`protected _items: IProduct[]`**  
  Массив товаров, находящихся в корзине. Используется для внутреннего хранения данных.

- **`public eventEmitter: EventEmitter`**  
  Экземпляр класса событий, используется для оповещения других частей приложения о событиях, связанных с корзиной (например, изменение содержимого).

#### Конструктор

```ts
constructor(eventEmitter: EventEmitter)
```
 принимает объект eventEmitter и сохраняет его в свойстве экземпляра. Это позволяет экземпляру класса отправлять события (например, при изменении корзины) другим частям приложения.


#### Методы

- **`addProduct(product: IProduct): void`**  
  Добавляет товар в корзину, если он валиден и ещё не был добавлен. 
- **`removeProduct(product: IProduct): void`**  
  Удаляет товар из корзины по его идентификатору.
- **`getCount(): number`**  
  Возвращает количество валидных товаров в корзине.
- **`getTotal(): number`**  
  Возвращает общую стоимость всех валидных товаров в корзине.
- **`clear(): void`**  
  Очищает корзину, удаляя все товары.`.
- **`cleanInvalidItems(): void`**  
  Удаляет из корзины все невалидные товары.

---


#### DataCatalog

**Назначение:**

Класс `DataCatalog` отвечает за хранение и управление данными каталога товаров, а также за выбор товара для предпросмотра. Он реализует интерфейс `IDataCatalogContract` и используется для передачи данных между слоями приложения.

**Поля класса:**
- `_products: IProduct[]` — защищённое поле, массив всех товаров каталога.
- `selectedProduct: IProduct | null` — выбранный товар для предпросмотра.
- `events: IEvents` — объект для работы с событиями.

**Конструктор:**
- `constructor(events: IEvents)` — принимает объект событий и сохраняет его в поле класса. Это позволяет классу оповещать другие части приложения о получении новых товаров или выборе товара для предпросмотра.

**Методы:**
- `setPreview(product: IProduct): void` — устанавливает выбранный товар для предпросмотра и эмиттит событие с этим товаром.
- `isValidProduct(item: IProduct): boolean` — проверяет, что переданный объект товара валиден.

---


#### OrderForm

**Назначение:**

Класс `OrderForm` отвечает за хранение и управление данными формы заказа, а также за подготовку данных для оформления заказа. Он реализует интерфейс `IOrderFormContract` и используется для сбора и очистки пользовательских данных, необходимых для оформления заказа.

**Поля класса:**
- `payment: string` — способ оплаты.
- `email: string` — email пользователя.
- `phone: string` — телефон пользователя.
- `address: string` — адрес доставки.
- `events: IEvents` — объект для работы с событиями.

**Конструктор:**
- `constructor(events: IEvents)` — принимает объект событий и сохраняет его в поле класса. Это позволяет классу оповещать другие части приложения о действиях пользователя, если потребуется.

**Методы:**
- `setAddress(value: string): void` — устанавливает адрес доставки.
- `setPayment(value: string): void` — устанавливает способ оплаты.
- `setEmail(value: string): void` — устанавливает email пользователя.
- `setPhone(value: string): void` — устанавливает телефон пользователя.
- `clear(): void` — очищает все поля формы.
- `getOrderRequest(total: number, items: string[]): IOrderRequest` — формирует и возвращает объект заказа для отправки на сервер, используя текущие значения полей, итоговую сумму и список идентификаторов товаров.
- `validateAddress(address: string, payment?: string): string | null`- проверяет корректность адреса доставки.
- `validatePayment(payment: string): string | null` - проверяет, , что способ оплаты выбран.
- `validateEmail(email: string): string | null` -  проверяет, что email указан.
 - `validatePhone(phone: string): string | null` - проверяет, что телефон указан.
 - `validateAll(): { errors: Record<string, string>, isValid: boolean }`  - проверяет все поля формы.
 - `validatePaymentStep()` - проверяет поля для шага оплаты.
 - `validateContactsStep()` - проверяет поля для шага контактов


### Классы View

## 🧺 Класс: BasketView

**Назначение:**

Класс `BasketView` управляет отображением содержимого корзины, обновлением списка товаров, итоговой суммы и счетчика в шапке. Работает только с DOM-элементами, не хранит данные о товарах.

---

### 🏷️ Поля класса

- **`element`** — корневой DOM-элемент корзины
- **`titleElement`** — элемент заголовка модального окна корзины
- **`listElement`** — элемент для списка товаров в корзине
- **`orderButton`** — кнопка оформления заказа
- **`priceElement`** — элемент для отображения итоговой суммы заказа

---

### 🛠️ Конструктор

- Клонирует содержимое шаблона и создаёт корневой элемент корзины
- Инициализирует ссылки на все необходимые DOM-элементы
- Добавляет обработчик события на кнопку заказа, который эмитит событие `order:open`
- Инициализирует пустой массив товаров

---

### ⚙️ Методы

- **`set items(items: HTMLElement[])`**  
  Сеттер для установки HTML-элементов товаров в корзину:
  - Если есть товары — заменяет содержимое списка и активирует кнопку заказа
  - Если корзина пуста — деактивирует кнопку и показывает сообщение "Корзина пуста"

- **`setItems(items: IProduct[], onRemove: (item: IProduct) => void, tplCardBasket: HTMLTemplateElement)`**  
  Основной метод для установки товаров в корзину:
  - Принимает массив товаров, функцию удаления и шаблон карточки товара
  - Создаёт HTML-элементы для каждого товара с данными (индекс, название, цена)
  - Добавляет обработчики удаления товаров
  - Управляет состоянием кнопки заказа и отображением пустой корзины

- **`updateTotalPrice(sum: number)`**  
  Обновляет отображение общей стоимости корзины в формате "X синапсов"

- **`render()`**  
  Метод рендеринга:
  - Устанавливает заголовок "Корзина"
  - Возвращает корневой элемент для вставки в DOM

---

### 📄 Интерфейс IBasket

Класс реализует интерфейс `IBasket`, который определяет контракт для компонента корзины с обязательными полями и методами.


## 🪟 Класс: OrderModalView

**Назначение:**

Класс `OrderModalView` управляет отображением модального окна заказа. Его задача — показывать переданный ему контент (например, форму заказа или форму контактов) внутри выделенного контейнера на странице. Класс не содержит бизнес-логики и не хранит данные — он только визуализирует содержимое модального окна.

---

### 🏷️ Поля класса

- **`container: HTMLElement`**  
  Приватное поле, в котором хранится DOM-элемент-контейнер для модального окна.

---

### 🛠️ Методы класса

- **`constructor(container: HTMLElement)`**  
  Принимает контейнер, в который будет рендериться содержимое модального окна.

- **`show(content: HTMLElement): void`**  
  Очищает контейнер и вставляет в него переданный элемент `content`. Используется для отображения любой разметки (например, формы заказа или формы контактов) внутри модального окна.



---

### 🪟 Класс: ModalView

**Назначение:**

Класс `ModalView` представляет собой **View-компонент для управления модальными окнами** в приложении. Он реализует интерфейс `IModal` и отвечает за отображение, открытие, закрытие модальных окон с различным содержимым.

#### 🏷️ Поля класса

**Защищенные поля (`protected`):**
- **`modalContainer: HTMLElement`** — основной контейнер модального окна
- **`closeButton: HTMLButtonElement`** — кнопка закрытия модального окна
- **`_content: HTMLElement`** — контейнер для содержимого модального окна
- **`_pageWrapper: HTMLElement`** — обертка страницы для блокировки прокрутки
- **`events: IEvents`** — система событий для коммуникации с другими компонентами

**Приватные поля (`private`):**
- **`_isSuccess: boolean`** — флаг, указывающий является ли модальное окно окном успешного заказа

#### 🏗️ Конструктор

```typescript
constructor(modalContainer: HTMLElement, pageWrapper: HTMLElement, protected events: IEvents)
```

**Что происходит в конструкторе:**
Конструктор класса ModalView инициализирует ссылки на DOM-элементы модального окна, настраивает обработчики событий для закрытия модалки (по кнопке, фону, Escape) и предотвращает закрытие при клике по содержимому. Всё необходимое для работы модального окна настраивается сразу при создании экземпляра.
#### ⚙️ Методы

**Основные методы:**
- **`open(): void`** — открывает модальное окно, добавляет класс активности и блокирует страницу
- **`close(): void`** — закрывает модальное окно, очищает содержимое и разблокирует страницу
- **`render(): HTMLElement`** — открывает модальное окно и возвращает DOM-элемент

#### 📄 Интерфейс IModal

Класс реализует интерфейс `IModal`, который определяет контракт для модального окна

---

#### 🏷️ Поля класса

- **galleryContainer: HTMLElement**  
  Контейнер (DOM-элемент), в который будут добавляться карточки товаров. Используется для отображения списка товаров на главной странице.

- **basketCounter: HTMLElement**  
  DOM-элемент, отображающий количество товаров в корзине. Используется для визуального обновления счетчика корзины.

---

#### 🏗️ Конструктор

```ts
constructor(root: HTMLElement, basketCounter: HTMLElement)
```
- **root** — DOM-элемент, который будет использоваться как контейнер для карточек товаров.
- **basketCounter** — DOM-элемент для отображения количества товаров в корзине.

Конструктор сохраняет эти элементы в соответствующих полях класса для дальнейшего использования.

---

#### 🛠️ Методы

- **setProducts(cards: HTMLElement[])**  
  Принимает массив карточек товаров (DOM-элементов) и вызывает внутренний метод renderProducts для их отображения.

- **private renderProducts(cards: HTMLElement[])**  
  Очищает контейнер галереи и добавляет в него все переданные карточки товаров. Используется для обновления списка товаров на странице.

- **updateBasketCounter(count: number)**  
  Обновляет текстовое содержимое счетчика корзины, устанавливая текущее количество товаров.

---

## 🎯 HeaderView

**Назначение:**

Класс `HeaderView` управляет представлением заголовка страницы, включая кнопку корзины и счетчик товаров. Отвечает за отображение количества товаров в корзине и обработку кликов по кнопке корзины для открытия модального окна.

---

### 🏷️ Поля класса

- **`headerElement: HTMLElement`** — DOM элемент заголовка страницы
- **`basketButton: HTMLButtonElement`** — кнопка корзины в заголовке
- **`basketCounter: HTMLElement`** — элемент счетчика товаров в корзине
- **`events: IEvents`** — объект для работы с событиями


### 🛠️ Конструктор

```typescript
constructor(headerElement: HTMLElement, events: IEvents)
```

**Параметры:**
- `headerElement: HTMLElement` — DOM элемент заголовка
- `events: IEvents` — объект для работы с событиями

**Логика конструктоа:**
1. Сохраняет переданные параметры в приватные поля
2. Находит кнопку корзины (`.header__basket`) в DOM
3. Находит счетчик товаров в корзине (`.header__basket-counter`) в DOM
4. Добавляет обработчик клика на кнопку корзины, который эмитит событие `'basket:open'`


### ⚙️ Методы

- **`updateBasketCounter(count: number): void`**  
  Обновляет отображение количества товаров в корзине:
  - Принимает число товаров
  - Устанавливает текстовое содержимое счетчика

---

## 🖼️ Класс: MainPageView

**Назначение:**

Класс `MainPageView` управляет отображением карточек товаров на главной странице и обновлением счетчика корзины. Работает только с DOM-элементами, не хранит данные о товарах.

---

### 🏷️ Поля класса

- **`galleryContainer: HTMLElement`**  
  Контейнер для отображения карточек товаров (галереи). Ссылается на DOM-элемент, в который будут добавляться карточки товаров.

- **`basketCounter: HTMLElement`**  
  DOM-элемент, отображающий количество товаров в корзине (счётчик корзины).

---

### 🏗️ Конструктор

```ts
constructor(root: HTMLElement, basketCounter: HTMLElement)
```
- **`root`** — DOM-элемент, который будет использоваться как контейнер для карточек товаров (галереи).
- **`basketCounter`** — DOM-элемент, в котором отображается количество товаров в корзине.

> В конструкторе значения этих элементов сохраняются в соответствующие приватные поля класса.

---

### 🛠️ Методы

- **`setProducts(cards: HTMLElement[])`**  
  Принимает массив карточек товаров (DOM-элементов) и вызывает приватный метод `renderProducts` для их отображения в галерее.

- **`private renderProducts(cards: HTMLElement[])`**  
  Очищает контейнер галереи и добавляет в него все переданные карточки товаров.

- **`updateBasketCounter(count: number)`**  
  Обновляет текстовое содержимое элемента-счётчика корзины, устанавливая туда текущее количество товаров.

---


#### FormView

## 📝 Класс: FormView

**Назначение:**

Класс `FormView` реализует представление формы, управляет её элементами, обработкой ошибок и состоянием кнопки отправки. Используется для работы с формами на основе шаблона (template) и событийной системы.

---

### 🧩 Поля класса

- **form: HTMLFormElement**  
  Основной DOM-элемент формы, клонируется из переданного шаблона.

- **inputs: HTMLInputElement[]**  
  Массив всех input-элементов внутри формы (по селектору).

- **submitButton: HTMLButtonElement**  
  Кнопка отправки формы.

- **errors: HTMLElement**  
  DOM-элемент для отображения ошибок.

- **protected events: IEvents**  
  Ссылка на объект событийной системы (интерфейс IEvents).


### 🏗️ Конструктор

```ts
constructor(
  template: HTMLTemplateElement,
  events: IEvents,
  selectors: {
    form: string;
    input: string;
    button: string;
    errors: string;
  }
)
```

> Конструктор сохраняет ссылку на events, находит и клонирует элемент формы из шаблона, а также находит все input-элементы, кнопку отправки и блок ошибок внутри формы.


### 🛠️ Методы

- **set valid(value: boolean)**  
  Включает или выключает кнопку отправки формы (делает disabled).

- **setError(message: string)**  
  Устанавливает текст ошибки в блок ошибок и показывает/скрывает его.

- **clearError()**  
  Очищает сообщение об ошибке (вызывает setError с пустой строкой).

- **get formElement()**  
  Геттер для получения DOM-элемента формы.

- **get errorElement()**  
  Геттер для получения DOM-элемента ошибок.

- **get submitBtn()**  
  Геттер для получения DOM-элемента кнопки отправки.

- **render()**  
  Возвращает DOM-элемент формы для вставки в документ.

---

## 🪙 Класс: PaymentModalView

**Назначение:**

Класс `PaymentModalView` реализует представление (View) для шага выбора способа оплаты и ввода адреса доставки в процессе оформления заказа. Он наследуется от базового класса `FormView`, расширяя его функциональность для работы с кнопками выбора оплаты и полем адреса. Класс управляет DOM-элементами формы, обработкой пользовательских событий и визуализацией ошибок.

---

### 🏷️ Поля класса

- **buttonAll: HTMLButtonElement[]**
  - Массив всех кнопок выбора способа оплаты (например, "Онлайн", "При получении").
  - Используется для управления их состоянием (активная/неактивная).

- **private currentAddress: string**
  - Текущее значение поля адреса доставки.
  - Обновляется при вводе пользователем.

- **private currentPayment: string**
  - Текущий выбранный способ оплаты.
  - Обновляется при выборе пользователем соответствующей кнопки.

---

### 🛠️ Методы класса

- **constructor(template: HTMLTemplateElement, events: IEvents)**
  - Вызывает конструктор родительского класса `FormView`, передавая селекторы для формы, инпутов, кнопки и блока ошибок.
  - Находит все кнопки выбора оплаты (`.button_alt`) и сохраняет их в `buttonAll`.
  - Добавляет обработчики клика на каждую кнопку оплаты:
  - При клике обновляет `currentPayment` и эмитит событие `'payment:select'` с выбранным методом.
  - Добавляет обработчик ввода в поле адреса:
   - При изменении адреса обновляет `currentAddress` и эмитит событие `'order:changeAddress'` с новым значением.
- Добавляет обработчик отправки формы:
- При отправке формы предотвращает стандартное поведение и эмитит событие `'order:submit'`.

- **updatePaymentSelection(method: string): void**
  - Обновляет визуальное состояние кнопок оплаты.
  - Активирует кнопку, соответствующую выбранному методу, и деактивирует остальные.

- **setError(error: string): void**
  - Показывает ошибку в форме, используя метод родительского класса.

- **setSubmitEnabled(enabled: boolean): void**
  - Включает или отключает кнопку отправки формы (submit), используя метод родительского класса.


---

## 📇 Класс: ContactsModalView

**Назначение:**

Класс `ContactsModalView` реализует представление (View) для шага ввода контактных данных пользователя (например, email и телефон) в процессе оформления заказа. Он наследуется от базового класса `FormView` и расширяет его функциональность для работы с контактной формой. Класс управляет DOM-элементами формы, обработкой пользовательских событий и отображением ошибок.

---

### 🏷️ Поля класса

- **inputs: HTMLInputElement[]**  
  Массив всех input-элементов формы (например, для email и телефона).  
  Используется для отслеживания изменений в каждом поле.

- **form: HTMLFormElement**  
  DOM-элемент формы контактов.

- **submitButton: HTMLButtonElement**  
  Кнопка отправки формы.

- **errors: HTMLElement**  
  DOM-элемент для отображения ошибок.

- **events: IEvents**  
  Объект событийной системы для коммуникации с другими частями приложения.

---

### 🛠️ Методы класса

- **constructor(template: HTMLTemplateElement, events: IEvents)**
  - Вызывает конструктор родительского класса `FormView`, передавая селекторы для формы, инпутов, кнопки и блока ошибок.
  - Добавляет обработчики ввода на все поля формы:
    - При изменении любого поля эмитит событие `'contacts:changeInput'` с именем и значением поля.
  - Добавляет обработчик отправки формы:
    - При отправке формы предотвращает стандартное поведение и эмитит событие `'contacts:submit'`.

- **setError(error: string): void**
  - Показывает ошибку в форме, используя метод родительского класса.

- **setSubmitEnabled(enabled: boolean): void**
  - Включает или отключает кнопку отправки формы (submit), используя метод родительского класса.

---

### 🎉 Класс: OrderSuccessModal

**Назначение:**

Класс `OrderSuccessModal` реализует модальное окно, которое отображается после успешного оформления заказа. Оно показывает пользователю сообщение об успешной оплате и сумму списанных синапсов, а также содержит кнопку для закрытия модального окна.

### 🏷️ Поля класса

- **success: HTMLElement**  
  Корневой элемент модального окна, клонируемый из переданного шаблона.

- **description: HTMLElement**  
  Элемент внутри модального окна, в который будет подставляться текст с суммой списанных синапсов.

- **button: HTMLButtonElement**  
  Кнопка закрытия модального окна.


### 🏗️ Конструктор

```typescript
constructor(template: HTMLTemplateElement, events: IEvents)
```
Клонирует разметку модального окна из шаблона, находит элементы описания и кнопки закрытия, и добавляет обработчик клика на кнопку для закрытия окна через событие.


### 🛠️ Методы

- **render(total: number): HTMLElement**  
  Обновляет текст в элементе описания, подставляя сумму списанных синапсов, и возвращает корневой элемент модального окна для дальнейшего отображения.

---

## 🛍️ Класс: ProductView

**Назначение:**

Класс `ProductView` реализует интерфейс `IPage` и отвечает за отображение карточки товара на странице. Он создает DOM-элемент карточки на основе шаблона, наполняет его данными о товаре и управляет отображением категории, названия, изображения и цены.

---

### 🏷️ Поля класса

- **_cardElement**: `HTMLElement`  
  Корневой элемент карточки товара, клонируется из шаблона.
- **_cardCategory**: `HTMLElement`  
  Элемент, отображающий категорию товара.
- **_cardTitle**: `HTMLElement`  
  Элемент, отображающий название товара.
- **_cardImage**: `HTMLImageElement`  
  Элемент изображения товара.
- **_cardPrice**: `HTMLElement`  
  Элемент, отображающий цену товара.
- **_colors**: `Record<string, string>`  
  Словарь для сопоставления русских названий категорий с CSS-классами для стилизации.
- **events**: `IEvents`  
  Экземпляр событийного менеджера (передается в конструктор).



### 🏗️ Конструктор

В конструкторе:
Клонируется шаблон карточки товара.
Получаются ссылки на элементы категории, названия, изображения и цены внутри карточки.
Если передан обработчик клика, он добавляется на карточку.



### 🛠️ Методы

- **setTextContent(element, value)**  
  Устанавливает текстовое содержимое для переданного элемента.
- **set cardCategory(value: string)**  
  Устанавливает категорию товара, а также соответствующий CSS-класс для стилизации категории.
- **formatPrice(value: number | null)**  
  Форматирует цену: если цена `null`, возвращает "Бесценно", иначе — `<число> синапсов`.
- **render(data: IProduct): HTMLElement**  
  Основной метод для отображения карточки товара:  
  — Устанавливает категорию, название, изображение и цену на основе переданных данных.  
  — Возвращает готовый DOM-элемент карточки.

---


## 📝Класс: ProductPreview
 
🧩 **Назначение**
 Класс ProductPreview отвечает за отображение карточки товара в виде предварительного просмотра и управление её состоянием (например, добавление/удаление товара из корзины). Наследует ProductView и реализует интерфейс ICard.
 
🏷️ **Поля класса**
  - text: HTMLElement — элемент для описания товара
  - button: HTMLElement — кнопка действия (купить/удалить)
  - currentProduct: IProduct | null — текущий отображаемый товар
  - currentBasketItems: IProduct[] — товары, находящиеся в корзине
 

 
🏗️ **Конструктор**
  - Вызывает super() для инициализации базовых свойств ProductView
  - Находит элементы .card__text и .card__button
  - Вешает обработчик на кнопку: при клике добавляет или удаляет товар из корзины через события
 

 
🛠️ **Методы класса**
   - render(data: IProduct, basketItems: IProduct[] = []): HTMLElement
   - Сохраняет текущий товар и корзину
   - Устанавливает заголовок, изображение, цену, описание
   - Определяет, находится ли товар в корзине
   - Меняет текст и состояние кнопки в зависимости от доступности и наличия в корзине
   - Возвращает DOM-элемент карточки
 
  ---
 
### Классы Presenter

## 🧑‍💼 Класс: AppPresenter

**Назначение:**  
AppPresenter — главный презентер приложения, который связывает между собой модели данных, представления (View) и обработку событий. Управляет отображением товаров, корзины, модальных окон, обработкой заказов и взаимодействием между компонентами через EventEmitter.

---

### 🏷️ Поля класса

- **basketView**: BasketView  
  Представление корзины.
- **eventEmitter**: EventEmitter  
  Система событий для коммуникации между компонентами.
- **apiModel**: ApiModel  
  Модель для работы с API (загрузка товаров и т.д.).
- **basketModel**: BasketModel  
  Модель корзины (хранит товары, их количество и т.д.).
- **dataCatalog**: DataCatalog  
  Каталог товаров (данные о продуктах).
- **modal**: ModalView  
  Представление модального окна.
- **orderForm**: OrderForm  
  Модель формы заказа (адрес, оплата, контакты).
- **headerView**: HeaderView  
  Представление шапки сайта (например, счетчик корзины).
- **currentPaymentModalView**: PaymentModalView | null  
  Текущее представление модального окна оплаты.
- **currentContactsModalView**: ContactsModalView | null  
  Текущее представление модального окна контактов.
- **mainPageView**: MainPageView  
  Представление главной страницы (каталог товаров).
- **tplCardCatalog**: HTMLTemplateElement  
  Шаблон карточки товара для каталога.
- **tplProductPreview**: HTMLTemplateElement  
  Шаблон предпросмотра товара.
- **tplCardBasket**: HTMLTemplateElement  
  Шаблон карточки товара в корзине.
- **tplOrder**: HTMLTemplateElement  
  Шаблон формы заказа.
- **tplContacts**: HTMLTemplateElement  
  Шаблон формы контактов.
- **tplSuccess**: HTMLTemplateElement  
  Шаблон успешного оформления заказа.
- **orderModalView**: OrderModalView  
  Представление модального окна заказа.

---

### 🏗️ Конструктор

constructor(
  eventEmitter: EventEmitter,
  apiModel: ApiModel,
  basketModel: BasketModel,
  dataCatalog: DataCatalog,
  orderForm: OrderForm,
  basketView: BasketView,
  modal: ModalView,
  headerView: HeaderView,
  mainPageView: MainPageView,
  tplCardCatalog: HTMLTemplateElement,
  templates: { ... },
  modalContent: HTMLElement
)

Конструктор принимает все зависимости и шаблоны, инициализирует поле orderModalView, вызывает методы инициализации данных и установки обработчиков событий.

---

### 🛠️ Основные методы

Основные методы
- init (private, async):
Загружает список товаров с помощью apiModel, сохраняет их в dataCatalog, отображает на главной странице, обновляет счетчик корзины.
- setupEventListeners (private):
Подписывает обработчики на события, связанные с товарами, корзиной, модальными окнами и формами.
- handleBasketChanged:
Обновляет счетчик корзины и, если открыта корзина, обновляет её содержимое.
- handleCardSelect:
Открывает модальное окно с подробной информацией о товаре.
- handleAddToBasket:
Добавляет товар в корзину и закрывает модальное окно.
- handleOrderOpen:
Очищает корзину от невалидных товаров и открывает модальное окно оформления заказа.
- handleSuccessClose:
Закрывает модальное окно успешного заказа, очищает корзину и форму заказа, обновляет счетчик.
- handleModalClose:
Очищает ошибки в текущих модальных окнах оплаты и контактов.
- handlePaymentSelect:
Устанавливает выбранный способ оплаты и обновляет валидацию формы оплаты.
- handleChangeAddress:
Устанавливает адрес доставки и обновляет валидацию формы оплаты.
- handleContactsInput:
Устанавливает email или телефон в форме заказа и обновляет валидацию формы контактов.
- handleRemoveFromBasket:
Удаляет товар из корзины и открывает модальное окно с этим товаром.
- updateBasketCounter:
Обновляет счетчик товаров в корзине в шапке сайта.
- updateBasketModal:
Очищает корзину от невалидных товаров, обновляет список товаров и общую сумму в корзине, отображает корзину в модальном окне.
- openProductModal:
Открывает модальное окно с подробной информацией о товаре.
- openOrderModal:
Открывает модальное окно оформления заказа, инициализирует формы оплаты и контактов, настраивает обработчики отправки форм.
- openContactsModal:
Открывает модальное окно для ввода контактных данных.
- openSuccessModal:
Открывает модальное окно успешного оформления заказа.
- updatePaymentModalValidation:
Обновляет состояние и ошибки формы оплаты.
- updateContactsModalValidation:
Обновляет состояние и ошибки формы контактов.


## 📐 Интерфейсы и типы (из src/types/index.ts)

```ts
// Описывает товар в каталоге
export interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}

// Описывает действия для элементов (например, обработчик клика)
export interface IActions {
  onClick: (event: MouseEvent) => void;
}

// Описывает данные формы заказа (адрес, телефон, email, способ оплаты, сумма)
export interface IOrderForm {
  address?: string;
  phone?: string;
  email?: string;
  payment?: string;
  total?: string | number;
}

// Описывает заказ, включая список id товаров
export interface IOrder extends IOrderForm {
  items: string[];
}

// Описывает структуру заказа для отправки на сервер
export interface IOrderRequest {
  email: string;
  phone: string;
  payment: string;
  address: string;
  items: string[];
  total: number;
}

// Описывает результат оформления заказа (id заказа и итоговая сумма)
export interface IOrderResult {
  id: string;
  total: number;
}

// Описывает возможные ошибки валидации формы заказа
export type FormErrors = Partial<Record<keyof IOrder, string>>;
```

---
