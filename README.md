# Проектная работа "Веб-ларёк" 🛒

## 📝 Описание

Интернет-магазин с товарами для веб-разработчиков — Web-ларёк. В нём можно посмотреть каталог товаров, добавить товары в корзину и сделать заказ. В проекте используется классовая структура и архитектура MVP.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
# или
yarn
```

### Запуск в режиме разработки

```bash
npm run start
# или
yarn start
```

## 🗂️ Ключевые файлы

- `src/pages/index.html` — HTML-файл главной страницы
- `src/types/index.ts` — файл с типами
- `src/index.ts` — точка входа приложения
- `src/scss/styles.scss` — корневой файл стилей
- `src/utils/constants.ts` — файл с константами
- `src/utils/utils.ts` — файл с утилитами

## 🛠️ Технологический стек

- **Frontend:** HTML, SCSS, TypeScript
- **Сборка:** Webpack, PostCSS, Babel
- **Пакетный менеджер:** npm/yarn
- **Форматирование:** Prettier
- **Среда выполнения:** Node.js

---

## 🏗️ Архитектура

В проекте используется классовая структура — сущности реализованы как классы, которые инкапсулируют данные и методы. Архитектура построена по принципу **MVP**:

- **Model** — отвечает за данные
- **View (Компоненты)** — отвечает за отображение
- **Presenter** — посредник между Model и View, управляет логикой взаимодействия, обрабатывает события, обновляет View.

### Классы Model

#### ApiModel

**Назначение:**

Класс `ApiModel` отвечает за взаимодействие с серверным API для получения списка товаров и оформления заказа. Он расширяет базовый класс `Api`, добавляя работу с CDN для изображений товаров и реализует методы для получения данных и отправки заказов.

**Поля класса:**
- `cdn: string` — адрес CDN-сервера, который используется для формирования абсолютных ссылок на изображения товаров.
- `items: IProduct[]` — массив товаров, полученных с сервера. По умолчанию — пустой массив.

**Конструктор:**
```ts
constructor(cdn: string, baseUrl: string, options?: RequestInit)
```
- `cdn` — строка с адресом CDN-сервера.
- `baseUrl` — базовый URL для API-запросов.
- `options` — дополнительные настройки для HTTP-запросов.

> Конструктор вызывает конструктор родительского класса `Api` с базовым URL и опциями, а также сохраняет адрес CDN в поле `cdn`.

**Методы:**
- `getProductList(): Promise<IProduct[]>` — получает список товаров с сервера. Возвращает промис с массивом товаров.
- `placeOrder(order: IOrderRequest): Promise<IOrderResult>` — отправляет заказ на сервер. Принимает объект заказа. Возвращает промис с результатом оформления заказа (`IOrderResult`).


#### BasketModel

**Назначение:**

Класс `BasketModel` реализует модель корзины интернет-магазина. Он отвечает за хранение и управление товарами, которые пользователь добавил в корзину.

**Поля класса:**
- `_items: IProduct[]` — защищённое поле, массив товаров в корзине. Используется для внутреннего хранения данных.

**Свойства:**
- `items` (getter/setter) — позволяет получить или установить массив товаров в корзине.

**Методы:**
- `addProduct(product: IProduct): void` — добавляет товар в корзину, если он валиден и ещё не был добавлен.
- `removeProduct(product: IProduct): void` — удаляет товар из корзины по его идентификатору.
- `getCount(): number` — возвращает количество валидных товаров в корзине.
- `getTotal(): number` — возвращает общую стоимость всех валидных товаров в корзине.
- `clear(): void` — очищает корзину, удаляя все товары.
- `cleanInvalidItems(): void` — удаляет из корзины все невалидные товары .




#### DataCatalog

**Назначение:**

Класс `DataCatalog` отвечает за хранение и управление данными каталога товаров, а также за выбор товара для предпросмотра. Он реализует интерфейс `IDataCatalogContract` и используется для передачи данных между слоями приложения.

**Поля класса:**
- `_products: IProduct[]` — защищённое поле, массив всех товаров каталога.
- `selectedProduct: IProduct | null` — выбранный товар для предпросмотра.
- `events: IEvents` — объект для работы с событиями (protected).

**Конструктор:**
- `constructor(events: IEvents)` — принимает объект событий и сохраняет его в поле класса. Это позволяет классу оповещать другие части приложения о получении новых товаров или выборе товара для предпросмотра.

**Свойства:**
- `products` (getter/setter) — позволяет получить или установить массив товаров в каталоге. При установке новых товаров эмиттит событие `'products:receive'`.

**Методы:**
- `setPreview(product: IProduct): void` — устанавливает выбранный товар для предпросмотра и эмиттит событие `'modalProduct:open'` с этим товаром.


#### OrderForm

**Назначение:**

Класс `OrderForm` отвечает за хранение и управление данными формы заказа, а также за подготовку данных для оформления заказа. Он реализует интерфейс `IOrderFormContract` и используется для сбора и очистки пользовательских данных, необходимых для оформления заказа.

**Поля класса:**
- `payment: string` — способ оплаты.
- `email: string` — email пользователя.
- `phone: string` — телефон пользователя.
- `address: string` — адрес доставки.
- `events: IEvents` — объект для работы с событиями (protected).

**Конструктор:**
- `constructor(events: IEvents)` — принимает объект событий и сохраняет его в поле класса. Это позволяет классу оповещать другие части приложения о действиях пользователя, если потребуется.

**Методы:**
- `setAddress(value: string): void` — устанавливает адрес доставки.
- `setPayment(value: string): void` — устанавливает способ оплаты.
- `setEmail(value: string): void` — устанавливает email пользователя.
- `setPhone(value: string): void` — устанавливает телефон пользователя.
- `clear(): void` — очищает все поля формы.
- `getOrderRequest(total: number, items: string[]): IOrderRequest` — формирует и возвращает объект заказа для отправки на сервер, используя текущие значения полей, итоговую сумму и список идентификаторов товаров.


#### BasketView

**Назначение:**
Управляет отображением содержимого корзины, обновлением списка товаров, итоговой суммы и счетчика в шапке. Работает только с DOM-элементами, не хранит данные о товарах.

**Поля класса:**
- `element` — корневой DOM-элемент корзины
- `titleElement` — элемент заголовка модального окна корзины
- `listElement` — элемент для списка товаров в корзине
- `orderButton` — кнопка оформления заказа
- `priceElement` — элемент для отображения итоговой суммы заказа
- `headerButton` — кнопка корзины в шапке сайта
- `headerCounter` — счетчик количества товаров в корзине в шапке

**Конструктор:**
- Клонирует шаблон корзины, находит и сохраняет нужные элементы
- Навешивает обработчики событий на кнопки (открытие корзины, оформление заказа)
- Связывает элементы шапки (кнопка и счетчик) с корзиной
- Инициализирует корзину в пустом состоянии

**Методы:**
- `set items(items: HTMLElement[])` — обновляет содержимое корзины.
- `updateHeaderCounter(value: number)` — обновляет счетчик товаров в шапке
- `updateTotalPrice(sum: number)` — обновляет итоговую сумму заказа
- `render()` — возвращает DOM-элемент корзины и устанавливает заголовок "Корзина".

#### MainPageView
**Назначение:**
Отвечает за отображение главной страницы с каталогом товаров и взаимодействие с корзиной. Управляет рендерингом карточек товаров, обработкой кликов по ним и по кнопке корзины.

**Поля класса:**
- `eventEmitter: EventEmitter` — объект для работы с событиями.
- `appPresenter: AppPresenter` — презентер приложения.

**Конструктор:**
- `constructor(eventEmitter: EventEmitter, apiModel: ApiModel, basketModel: BasketModel, dataCatalog: DataCatalog)` — принимает необходимые зависимости, создаёт AppPresenter и настраивает кнопку корзины.

**Методы:**
- `setProducts(cards: HTMLElement[])` — вызывает рендеринг карточек товаров.
- `renderProducts(cards: HTMLElement[])` — отрисовывает карточки товаров в контейнере.
- `setupBasketButton()` — навешивает обработчик на кнопку корзины.
- `updateBasketCounter(count: number)` — обновляет счетчик товаров в корзине.

---

#### FormView

**Назначение:**  
Класс `FormView` отвечает за отображение и управление формой на странице. Он инкапсулирует работу с DOM-элементами формы, обработку ошибок и управление состоянием кнопки отправки. Класс не хранит данные формы, а только управляет её визуальным состоянием и взаимодействием с пользователем.

**Поля класса:**
- `form: HTMLFormElement` — DOM-элемент формы, с которым работает данный экземпляр.
- `inputs: HTMLInputElement[]` — массив всех input-элементов внутри формы.
- `submitButton: HTMLButtonElement` — кнопка отправки формы.
- `errors: HTMLElement` — DOM-элемент для отображения ошибок формы.
- `events: IEvents` — экземпляр интерфейса событий, используемый для взаимодействия с внешними обработчиками.

**Конструктор:**
```ts
constructor(template: HTMLTemplateElement, events: IEvents, selectors: {
  form: string,
  input: string,
  button: string,
  errors: string
})
```
- `template` — HTML-шаблон, содержащий разметку формы.
- `events` — объект для работы с событиями.
- `selectors` — объект с CSS-селекторами для поиска элементов формы (сама форма, поля ввода, кнопка отправки, блок ошибок).

> Конструктор инициализирует экземпляр FormView, клонируя разметку формы из переданного шаблона, находит необходимые элементы по селекторам и сохраняет их в соответствующие поля. Также сохраняет ссылку на объект событий.

**Методы:**
- `set valid(value: boolean): void` — включает или отключает кнопку отправки формы в зависимости от валидности данных.
- `setError(message: string): void` — отображает сообщение об ошибке в блоке ошибок. Если сообщение пустое — скрывает блок ошибок.
- `clearError(): void` — очищает сообщение об ошибке (вызывает setError с пустой строкой).
- `get formElement(): HTMLFormElement` — возвращает DOM-элемент формы.
- `get errorElement(): HTMLElement` — возвращает DOM-элемент для ошибок.
- `get submitBtn(): HTMLButtonElement` — возвращает кнопку отправки формы.
- `render(): HTMLFormElement` — возвращает DOM-элемент формы для вставки в документ.


#### ModalView
**Назначение:**
Управляет отображением модального окна, открытием/закрытием, блокировкой страницы и динамическим контентом.

**Поля класса:**
- `modalContainer: HTMLElement` — контейнер модального окна.
- `closeButton: HTMLButtonElement` — кнопка закрытия.
- `_content: HTMLElement` — контейнер для контента.
- `_pageWrapper: HTMLElement` — основной контейнер страницы.
- `events: IEvents` — объект событий.

**Конструктор:**
- `constructor(modalContainer: HTMLElement, events: IEvents)` — принимает контейнер модального окна и объект событий. Назначает обработчики для закрытия окна и клавиши Escape.

**Методы:**
- `set content(value: HTMLElement | null)` — устанавливает содержимое модального окна.
- `open()` — открывает модальное окно.
- `close()` — закрывает модальное окно.
- `set locked(value: boolean)` — блокирует/разблокирует прокрутку страницы.
- `get locked(): boolean` — возвращает состояние блокировки.
- `render(): HTMLElement` — открывает и возвращает контейнер модального окна.

---

#### OrderModalView
**Назначение:**
OrderModalView управляет контейнером для этапов оформления заказа. Позволяет переключать отображение между этапами (оплата, контакты).

**Поля класса:**
- `container: HTMLElement` — контейнер модального окна.

**Конструктор:**
- `constructor(container: HTMLElement)` — принимает контейнер, в котором будет отображаться содержимое этапов.

**Методы:**
- `show(content: HTMLElement)` — очищает контейнер и вставляет переданный контент.

---

#### PaymentModalView (наследуется от FormView)
**Назначение:**
Компонент для этапа выбора способа оплаты и ввода адреса доставки. Управляет состоянием кнопок выбора оплаты и валидацией адреса.

**Поля класса:**
- `buttonAll: HTMLButtonElement[]` — все кнопки выбора способа оплаты.
- `paymentSelection: string` — выбранный способ оплаты.
- `touchedAddress: boolean` — был ли изменён адрес.

**Конструктор:**
- `constructor(template: HTMLTemplateElement, events: IEvents)` — вызывает конструктор FormView, находит кнопки оплаты, навешивает обработчики событий на кнопки и поля формы.

**Методы:**
- `updatePaymentSelection(method: string)` — обновляет выбранный способ оплаты и визуальное состояние кнопок.
- `updateButtonState()` — валидирует адрес и выбор оплаты, управляет ошибками и доступностью кнопки отправки.

---

#### ContactsModalView (наследуется от FormView)
**Назначение:**
Компонент для этапа ввода контактных данных (email, телефон). Валидирует поля и управляет ошибками.

**Поля класса:**
- `touchedEmail: boolean` — был ли изменён email.
- `touchedPhone: boolean` — был ли изменён телефон.

**Конструктор:**
- `constructor(template: HTMLTemplateElement, events: IEvents)` — вызывает конструктор FormView, навешивает обработчики на поля ввода и отправку формы.

**Методы:**
- `validateEmail(email: string): boolean` — проверяет корректность email.
- `validatePhone(phone: string): boolean` — проверяет корректность телефона.
- `updateButtonState()` — валидирует поля, управляет ошибками и доступностью кнопки отправки.

---

#### ProductPreview (наследуется от ProductView)
**Назначение:**
Показывает подробную информацию о товаре и управляет добавлением/удалением товара из корзины.

**Поля класса:**
- `text: HTMLElement` — элемент для описания товара.
- `button: HTMLElement` — кнопка для добавления/удаления товара из корзины.
- `currentProduct: IProduct | null` — текущий отображаемый товар.
- `events: IEvents` — объект событий.

**Конструктор:**
- `constructor(template: HTMLTemplateElement, events: IEvents, actions?: IActions)` — вызывает конструктор ProductView, находит элементы описания и кнопки, навешивает обработчик на кнопку.

**Методы:**
- `render(data: IProduct, basketItems?: IProduct[]): HTMLElement` — отображает товар, обновляет содержимое карточки, настраивает кнопку в зависимости от состояния корзины.

---

#### OrderSuccessModal
**Назначение:**
Показывает модальное окно успешного заказа с итоговой суммой и кнопкой закрытия.

**Поля класса:**
- `success: HTMLElement` — DOM-элемент окна успеха (клонируется из шаблона).
- `description: HTMLElement` — элемент для текста с итоговой суммой.
- `button: HTMLButtonElement` — кнопка закрытия окна.
- `events: IEvents` — объект событий.

**Конструктор:**
- `constructor(template: HTMLTemplateElement, events: IEvents)` — клонирует шаблон, находит необходимые элементы, навешивает обработчик на кнопку.

**Методы:**
- `render(total: number): HTMLElement` — устанавливает сумму и возвращает DOM-элемент окна.

---

#### ProductView
**Назначение:**
Отвечает за отображение карточки товара на странице каталога.

**Поля класса:**
- `_cardElement: HTMLElement` — DOM-элемент карточки.
- `_cardCategory: HTMLElement` — категория товара.
- `_cardTitle: HTMLElement` — название товара.
- `_cardImage: HTMLImageElement` — изображение товара.
- `_cardPrice: HTMLElement` — цена товара.
- `_colors: Record<string, string>` — соответствие категорий цветам.
- `events: IEvents` — объект событий.

**Конструктор:**
- `constructor(template: HTMLTemplateElement, events: IEvents, actions?: IActions)` — клонирует DOM-элемент карточки, настраивает обработчик клика, сохраняет ссылки на элементы.

**Методы:**
- `setTextContent(element, value)` — устанавливает текстовое содержимое.
- `set cardCategory(value: string)` — обновляет категорию и цвет.
- `formatPrice(value: number | null)` — форматирует цену.
- `render(data: IProduct): HTMLElement` — заполняет карточку данными и возвращает DOM-элемент.

---

#### ProductPreview
**Назначение** — класс для отображения подробной информации о товаре и управления его добавлением/удалением из корзины. Наследуется от ProductView.
**Поля класса:**
- text — элемент для отображения описания товара
- button — кнопка для добавления/удаления товара из корзины
- currentProduct — текущий отображаемый товар
**Конструктор:**
- constructor(template: HTMLTemplateElement, protected events: IEvents, actions?: IActions) — вызывает конструктор родителя, инициализирует поля text и button. Для button добавляется обработчик клика
**Методы:**
- render — отображает товар, обновляет содержимое карточки, настраивает кнопку

## Базовые классы:

### 📦 Класс Api
**Назначение:**
Базовый класс для работы с HTTP API. Инкапсулирует выполнение HTTP-запросов (GET, POST, PUT, DELETE) и обработку ответов сервера.

**Поля класса:**
- `baseUrl: string` — базовый URL для всех запросов.
- `options: RequestInit` — объект с настройками для fetch-запросов (заголовки, параметры и т.д.).

**Конструктор:**
- `constructor(baseUrl: string, options: RequestInit = {})` — принимает базовый URL и опции, настраивает заголовки по умолчанию.

**Методы:**
- `protected handleResponse(response: Response): Promise<object>` — обрабатывает ответ сервера: если успешно — возвращает JSON, иначе — выбрасывает ошибку.
- `get(uri: string)` — выполняет GET-запрос по указанному URI.
- `post(uri: string, data: object, method: ApiPostMethods = 'POST')` — выполняет POST/PUT/DELETE-запрос с передачей данных.

---

### 🔔 Класс EventEmitter
**Назначение:**
Брокер событий. Позволяет объектам обмениваться событиями без прямой зависимости друг от друга. Используется для связи между слоями приложения.

**Поля класса:**
- `_events: Map<EventName, Set<Subscriber>>` — карта событий и подписчиков.

**Конструктор:**
- `constructor()` — инициализирует карту событий.

**Методы:**
- `on(event: EventName, callback: Function)` — подписка на событие.
- `off(event: EventName, callback: Function)` — отписка от события.
- `emit(event: string, data?: any)` — инициирует событие с данными.
- `onAll(callback: (event: EmitterEvent) => void)` — слушать все события.
- `offAll()` — сбросить все обработчики.
- `trigger(event: string, context?: object)` — возвращает функцию-триггер, которая при вызове инициирует событие с аргументами.

---

### Классы Presenter

#### AppPresenter

**Назначение:**

Класс `AppPresenter` реализует паттерн Presenter (MVP) и управляет связью между моделями данных (корзина, каталог, заказ), представлениями и глобальным событийным механизмом. Он отвечает за обработку пользовательских событий.

**Поля класса:**
- `products: IProduct[]` — список товаров, полученных с сервера.
- `basketView: BasketView` — представление корзины, отвечает за отображение и взаимодействие с корзиной.
- `eventEmitter: EventEmitter` — глобальный событийный эмиттер для коммуникации между компонентами.
- `apiModel: ApiModel` — модель для работы с API (загрузка товаров и т.д.).
- `basketModel: BasketModel` — модель корзины, хранит и управляет товарами в корзине.
- `dataCatalog: DataCatalog` — модель каталога товаров.
- `modal: ModalView` — представление модального окна, универсальный контейнер для различных модальных представлений.
- `orderForm: OrderForm` — модель формы заказа, хранит и валидирует данные заказа.
- `currentPaymentModalView: any` — ссылка на текущее представление модального окна оплаты (используется для обновления состояния кнопок и ошибок).
- `currentContactsModalView: any` — ссылка на текущее представление модального окна контактов (используется для обновления состояния кнопок и ошибок).

**Конструктор:**
```ts
constructor(eventEmitter: EventEmitter, apiModel: ApiModel, basketModel: BasketModel, dataModel: DataCatalog)
```
- Сохраняет переданные зависимости.
- Создаёт экземпляр модели формы заказа.
- Инициализирует представление корзины и модального окна, связывая их с DOM-элементами.
- Запускает начальную загрузку товаров.
- Подписывается на события через.

**Методы:**
- `private isValidProduct(item: IProduct): boolean` — проверяет, валиден ли товар (наличие id и title).
- `private async init()` — загружает список товаров с сервера, фильтрует их и сохраняет в каталог.
- `private setupEventListeners()` — подписывается на события приложения (выбор товара, добавление/удаление из корзины, открытие модальных окон, изменение формы заказа и т.д.) и связывает их с соответствующими обработчиками.
- `private createBasketItemElement(...)` — создаёт DOM-элемент для товара в корзине.
- `private setupBasketButton()` — навешивает обработчик на кнопку корзины в шапке.
- `private updateBasketCounter()` — обновляет счётчик товаров в корзине в шапке.
- `private openProductModal(item: IProduct)` — открывает модальное окно с предпросмотром товара.
- `private openBasketModal()` — открывает модальное окно с содержимым корзины.
- `private openOrderModal()` — открывает модальное окно оформления заказа (выбор оплаты и ввод адреса).
- `private openContactsModal()` — открывает модальное окно для ввода контактных данных.
- `private openSuccessModal()` — открывает модальное окно с успешным завершением заказа.
- `private fillOrderForm()` — очищает невалидные товары из корзины перед оформлением заказа.
- `private showFormErrors(errors: Record<string, string>)` — показывает ошибки формы в DOM.
- `private getTemplate(selector: string): HTMLTemplateElement | null` — получает шаблон по селектору.
- `private handleFormErrors(...)` — управляет отображением ошибок и состоянием кнопки отправки формы.

---

## 📐 Интерфейсы и типы (из src/types/index.ts)

```ts
// Описывает товар в каталоге
export interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}

// Описывает действия для элементов (например, обработчик клика)
export interface IActions {
  onClick: (event: MouseEvent) => void;
}

// Описывает данные формы заказа (адрес, телефон, email, способ оплаты, сумма)
export interface IOrderForm {
  address?: string;
  phone?: string;
  email?: string;
  payment?: string;
  total?: string | number;
}

// Описывает заказ, включая список id товаров
export interface IOrder extends IOrderForm {
  items: string[];
}

// Описывает структуру заказа для отправки на сервер
export interface IOrderRequest {
  email: string;
  phone: string;
  payment: string;
  address: string;
  items: string[];
  total: number;
}

// Описывает результат оформления заказа (id заказа и итоговая сумма)
export interface IOrderResult {
  id: string;
  total: number;
}

// Описывает возможные ошибки валидации формы заказа
export type FormErrors = Partial<Record<keyof IOrder, string>>;
```

---



